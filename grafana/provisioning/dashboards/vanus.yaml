apiVersion: 1

providers:
  - name: vanus    # A uniquely identifiable name for the provider
    folder: Vanus # The folder where to place the dashboards
    type: file
    options:
      path: /home/grafana/dashboard